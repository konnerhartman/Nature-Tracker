<section>
  <h2>Your Animals</h2>
  <button type="submit" class="btn btn-warning rounded-circle"><a class="btn" href="/dashboard/new">+ Add Animal</a></button>
  {{#if animals.length}}
  </br>
  {{#each animals as |animal|}}
    {{> dashboardAI animal}}
  {{/each}}
</section>
{{/if}}



<script src="/js/addAnimal.js"></script>
{{!-- 
<div class="container d-flex justify-content-center">
  <div class="row">
    <div class="col-auto">
      <h2 style="color:rgb(119, 48, 12);">Welcome, {{name}}!</h2>
    </div>
  </div>

  <div class="row mt-5">
    <a href="/dashboard/new"><button
        type="submit"
        class="btn"
        style="background-color: ACC555; font-weight:bold; font-family:Cambria, Cochin, Georgia, Times, 'Times New Roman', serif; border:solid black 1px"
      >Add a Find!</button></a>

    {{#if animals.length}}
      <div class="col-md-12">
        <h3
          style="color: rgb(119, 48, 12); font-size:30px; font-family:Cambria, Cochin, Georgia, Times, 'Times New Roman', serif"
        >Current Animals:</h3>

        {{#each animals as |animal|}}
          <div
            class="row mb-2"
            style="background-color:rgba(250, 250, 250); border:solid black 2px; border-radius:5px; padding: 20px"
          >
            <div class="col-md-8">
              <h4><a
                  href="/animal/{{animal.id}}"
                  style="color: black; font-family:Cambria, Cochin, Georgia, Times, 'Times New Roman', serif"
                >{{animal.name}}</a></h4>
            </div>
            <div class="col-md-4 edit-post-form">
              <a href="/dashboard/edit"><button
                  class="btn btn-sm btn-primary"
                  data-id="{{animal.id}}"
                  style="background-color: darkslategray;"
                >EDIT</button></a>
            </div>
            <div class="col-md-4 animal-list">
              <br />
              <button
                class="btn btn-sm btn-danger"
                data-id="{{animal.id}}"
                style="background-color:rgb(107, 0, 0);"
              >DELETE</button>
            </div>
          </div>
        {{/each}}
      </div>
    {{/if}}
  </div>
</div>

<script src="./js/deleteAnimal.js"></script> --}}
