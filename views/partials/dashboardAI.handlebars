<div class="card">
    <div class="card-header">
       <h4><a class="link" href="/post/{{id}}">{{name}}</a></h4>
        <p>Posted by {{user.name}} on {{format_date created_at}}</p>
    </div>
        <p class="card-body">{{description}}</p>
</div>